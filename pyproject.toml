# Tooling config only. Home Assistant is not installed here — the integration
# is tested by symlinking custom_components/kaco_rs485 into an HA dev instance.
[tool.ruff]
line-length = 100
target-version = "py313"

[tool.ruff.lint]
select = ["E", "F", "I", "UP", "B", "SIM", "RUF"]
